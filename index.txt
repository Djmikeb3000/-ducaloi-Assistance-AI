<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portail Synapse Pro | Éducaloi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --educaloi-blue-dark: #004d71;
            --educaloi-cyan: #00b0d8;
            --bg-light: #f8fafc;
        }

        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            background-color: white;
            overflow-x: hidden;
        }

        /* --- LOGIN PAGE --- */
        #login-page {
            position: fixed;
            inset: 0;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-card {
            background: white;
            width: 100%;
            max-width: 400px;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        /* --- LAYOUT RESPONSIVE --- */
        #main-app {
            display: none; 
            min-height: 100vh;
            width: 100vw;
            display: flex;
            flex-direction: row;
        }

        /* SIDEBAR RESPONSIVE */
        .sidebar {
            width: 280px;
            background: white;
            border-right: 1px solid #e2e8f0;
            display: flex;
            flex-direction: column;
            transition: transform 0.3s ease;
            z-index: 50;
        }

        /* Menu Mobile Caché par défaut sur petit écran */
        @media (max-width: 1024px) {
            .sidebar {
                position: fixed;
                height: 100vh;
                transform: translateX(-100%);
            }
            .sidebar.open {
                transform: translateX(0);
            }
        }

        .nav-group { flex: 1; overflow-y: auto; padding: 10px 0; }
        .section-label { padding: 20px 25px 8px; font-size: 11px; font-weight: 700; color: #94a3b8; text-transform: uppercase; letter-spacing: 1px; }
        .nav-link { padding: 10px 25px; color: #475569; display: flex; justify-content: space-between; align-items: center; font-size: 14px; cursor: pointer; }
        .nav-link.active { background: #f1f5f9; color: var(--educaloi-blue-dark); font-weight: 700; border-right: 4px solid var(--educaloi-cyan); }

        /* CONTENT AREA */
        .content-area {
            flex: 1;
            display: flex;
            flex-direction: column;
            background: #f8fafc;
            min-width: 0;
        }

        /* TOP NAVBAR RESPONSIVE */
        .top-navbar {
            height: 65px;
            background: white;
            border-bottom: 1px solid #e2e8f0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 20px;
            position: sticky;
            top: 0;
            z-index: 40;
        }

        .hero {
            background: linear-gradient(rgba(0, 77, 113, 0.95), rgba(0, 176, 216, 0.85)), url('https://www.transparenttextures.com/patterns/cubes.png');
            padding: 40px 20px 80px;
            color: white;
            text-align: center;
        }

        @media (min-width: 768px) {
            .hero { padding: 60px 40px 100px; text-align: left; }
        }

        .captation-container {
            margin: -50px 15px 40px;
            background: white;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.05);
            border: 1px solid #e2e8f0;
        }

        @media (min-width: 768px) {
            .captation-container { margin: -60px 40px 40px; padding: 40px; }
        }

        textarea {
            width: 100%;
            height: 180px;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 15px;
            font-size: 16px;
            margin: 20px 0;
            resize: none;
            background: #fafafa;
        }

        /* Overlay pour fermer le menu mobile */
        #overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.5);
            z-index: 45;
        }
    </style>
</head>
<body>

<div id="overlay" onclick="toggleMenu()"></div>

<div id="login-page">
    <div class="login-card">
        <div style="font-weight: 900; color: var(--educaloi-blue-dark); font-size: 22px; margin-bottom: 5px;">Éducaloi Assistance AI</div>
        <p style="font-size: 10px; color: #64748b; margin-bottom: 25px; text-transform: uppercase; letter-spacing: 1px;">Portail Avocat-Conseiller</p>
        <input type="text" placeholder="Utilisateur" class="w-full p-3 mb-3 border rounded-lg" value="sroy@educaloi.qc.ca">
        <input type="password" placeholder="Mot de passe" class="w-full p-3 mb-4 border rounded-lg" value="••••••••">
        <button onclick="login()" class="w-full bg-[#00b0d8] text-white p-4 rounded-lg font-bold">ACCÉDER AU PORTAIL</button>
    </div>
</div>

<div id="main-app" style="display: none;">
    
    <aside id="sidebar" class="sidebar">
        <div class="p-6 border-b flex justify-between items-center">
            <div style="font-weight: 900; color: var(--educaloi-blue-dark); font-size: 16px;">Éducaloi AI</div>
            <button class="lg:hidden text-slate-400" onclick="toggleMenu()"><i class="fas fa-times"></i></button>
        </div>
        <div class="nav-group">
            <div class="section-label">Outils</div>
            <div class="nav-link active"><span><i class="fas fa-microchip mr-2 text-cyan-500"></i> Aide au conseil</span></div>
            
            <div class="section-label">Dossiers</div>
            <div class="nav-link"><span>📁 #2026-104</span> <span class="bg-red-500 text-white text-[9px] px-1.5 py-0.5 rounded font-black">URGENT</span></div>
            <div class="nav-link"><span>📁 #2026-112</span></div>
            
            <div class="section-label">Historique</div>
            <div class="nav-link opacity-50 text-xs">📄 Abus financier (2h)</div>
            <div class="nav-link opacity-50 text-xs">📄 Mandat protection (12h)</div>
        </div>
    </aside>

    <main class="content-area">
        <nav class="top-navbar">
            <button class="lg:hidden text-slate-600 text-xl" onclick="toggleMenu()">
                <i class="fas fa-bars"></i>
            </button>
            
            <div class="flex items-center gap-4 ml-auto">
                <div class="hidden sm:flex flex-col text-right">
                    <span class="font-bold text-sm text-slate-800">Me Sophie Roy</span>
                    <span class="text-[10px] text-slate-400 font-bold uppercase">Roy & Associés</span>
                </div>
                <button onclick="location.reload()" class="bg-red-50 text-red-600 p-2 sm:px-4 sm:py-2 rounded-lg text-xs font-bold border border-red-100 flex items-center gap-2">
                    <i class="fas fa-power-off"></i> <span class="hidden sm:inline">DÉCONNEXION</span>
                </button>
            </div>
        </nav>

        <div class="hero">
            <span class="text-[10px] font-black tracking-widest opacity-70 uppercase">Système Synapse Pro</span>
            <h1 class="text-2xl sm:text-4xl font-black mt-2">Corrélation de faits</h1>
        </div>

        <div class="captation-container">
            <h3 class="font-black text-slate-800 text-lg">Analyse du litige</h3>
            <p class="text-slate-500 text-sm mt-1">Exposez les faits pour obtenir les pistes de solutions juridiques.</p>
            
            <textarea id="userInput" placeholder="Décrivez la situation ici..."></textarea>
            
            <div class="flex justify-end">
                <button onclick="analyser()" class="w-full sm:w-auto bg-[#00b0d8] text-white px-8 py-4 rounded-lg font-black shadow-lg shadow-cyan-100">
                    LANCER L'ANALYSE
                </button>
            </div>

            <div id="result-box" style="display: none;" class="mt-8 pt-8 border-t-2 border-slate-50">
                <div class="bg-cyan-50 border-l-4 border-cyan-500 p-6 rounded-r-xl">
                    <h4 class="text-cyan-800 font-black text-xs uppercase tracking-wider mb-2 italic">Rapport Synapse AI</h4>
                    <p id="result-text" class="text-slate-700 text-sm sm:text-base leading-relaxed"></p>
                </div>
            </div>
        </div>
    </main>
</div>

<script>
    function toggleMenu() {
        const sidebar = document.getElementById('sidebar');
        const overlay = document.getElementById('overlay');
        sidebar.classList.toggle('open');
        overlay.style.display = sidebar.classList.contains('open') ? 'block' : 'none';
    }

    function login() {
        document.getElementById('login-page').style.display = 'none';
        document.getElementById('main-app').style.display = 'flex';
    }

    function analyser() {
        const txt = document.getElementById('userInput').value;
        if(!txt) return;
        const box = document.getElementById('result-box');
        const res = document.getElementById('result-text');
        box.style.display = 'block';
        res.innerHTML = "<b>Analyse :</b> Risque de nullité de l'acte (Art. 1399 C.c.Q). <br><b>Piste :</b> Action en nullité pour cause d'inaptitude notoire. <br><b>Précédent :</b> <em>Succession Morin c. Lajoie (2023)</em>.";
    }
</script>

</body>
</html>